import 'package:flutter/material.dart';

class EditeMyInfoView extends StatelessWidget {
  const EditeMyInfoView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
