import 'package:flutter/material.dart';

class EditeVehicleInfo extends StatelessWidget {
  const EditeVehicleInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
